// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("iocron", "for iocron support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("iocron", "enable iocron support", "no");

if (PHP_IOCRON != "no") {
	EXTENSION("iocron", "iocron.c");
}

